# Core AI / ML — requires Python 3.10 or 3.11
torch>=2.1.0,<2.6
torchaudio>=2.1.0,<2.6
torchvision>=0.16.0,<0.21
transformers>=4.40.0
TTS

# Orchestration
langgraph

# UI
streamlit

# Audio / image utilities
numpy<2
librosa
soundfile
pillow
ffmpeg-python
huggingface-hub

# Notes:
# - Requires Python 3.10 or 3.11 (3.12+ has limited ML library support).
# - FFmpeg binary is a system dependency (not installed via pip).
# - SadTalker and Wav2Lip are external repos — clone them separately
#   and set SADTALKER_ROOT / WAV2LIP_ROOT environment variables.
